---
apiVersion: v1
kind: Service
metadata:
  name: nodeport-service
  labels:
    author: kbr
spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30072

  selector:
    app: proxy
...
