---
apiVersion: v1
kind: Service
metadata:
  name: loadbalance-service
  labels:
    owner: kbr
spec:
  type: LoadBalancer
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30008
  selector:
    app: proxy
    author: kbr

...
